# Copy this file to .env and fill in your actual values.

# Base URL of the external stage-work API server.
# Each stage appends its own sub-path defined in src/config/stageWorkConfig.ts
VITE_STAGE_WORK_API_BASE=http://localhost:9000
